export type IData = {
  url: string;
  hostname?: string;
  title?: string;
  icon?: string;
  manifest?: string;
};

export const STORAGE_RECENT = 'wt-recent';
export const STORAGE_FAVORITE = 'wt-favorite';
